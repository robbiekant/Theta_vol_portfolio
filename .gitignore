__pycache__/
*.pyc
.DS_Store

# Generated by `python -m thetavol.cli run`. Both are fully reproducible from the
# committed JSON snapshot, so they are not tracked:
#   the 174 KB task board (delivered as an artifact instead)
out/thetavol_taskboard.html
#   the flat IBKR CSV mirror pushed to Google Drive (`cli export` rebuilds it)
out/ibkr_*.csv
